[project]
name = "solver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch==2.11.0+cu126",
    "torchvision==0.26.0+cu126",
    "aim",
    "safetensors",
    "packaging",
]
